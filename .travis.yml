language: python
python:
  - 3.6

install:
  - pip install --upgrade setuptools
  - pip install tox

script:
  - tox -r

matrix:
  fast_finish: true

after_success:
  .tox/py36/bin/coveralls

notifications:
  irc:
    channels: "irc.freenode.org#invirtualenv"
    template:
      - "%{repository}@%{branch}: %{message} (%{build_url})"
    on_success: change
    on_failure: change
  email: false
